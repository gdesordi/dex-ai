interface:
  display_name: "Node Version Bump"
  short_description: "Executar bumps SemVer em projetos Node"
  default_prompt: "Use $node-version-bump para executar um bump major, minor ou patch, sincronizar manifests usados por workflows de release/tag, regenerar o lockfile e promover as alterações de [Não publicado] no changelog."
